---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: secretrotationpolicies.secrets.keysmith.io
spec:
  group: secrets.keysmith.io
  names:
    categories:
    - keysmith
    kind: SecretRotationPolicy
    listKind: SecretRotationPolicyList
    plural: secretrotationpolicies
    shortNames:
    - srp
    singular: secretrotationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider.name
      name: Provider
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.lastRotationTime
      name: Last Rotation
      type: string
    - jsonPath: .status.nextRotationTime
      name: Next Rotation
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecretRotationPolicy is the Schema for the secretrotationpolicies API.
          It defines a schedule and provider for automatically rotating a Kubernetes secret.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecretRotationPolicySpec defines the desired state of SecretRotationPolicy.
            properties:
              keys:
                description: Keys maps provider secret keys to Kubernetes secret data
                  keys.
                items:
                  description: KeyMapping maps a key from the provider secret to a
                    Kubernetes secret data key.
                  properties:
                    providerKey:
                      type: string
                    secretKey:
                      type: string
                  required:
                  - providerKey
                  - secretKey
                  type: object
                minItems: 1
                type: array
              notifications:
                description: Notifications configures webhook alerts on rotation events.
                items:
                  description: NotificationSpec configures a webhook notification
                    for rotation events.
                  properties:
                    events:
                      description: Events lists which rotation events trigger this
                        notification.
                      items:
                        description: NotificationEvent is a rotation lifecycle event
                          that can trigger a notification.
                        enum:
                        - RotationSucceeded
                        - RotationFailed
                        - RotationSkipped
                        type: string
                      minItems: 1
                      type: array
                    secretRef:
                      description: SecretRef reads the webhook URL from a Kubernetes
                        secret key.
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    url:
                      description: URL is the webhook endpoint. Mutually exclusive
                        with SecretRef.
                      type: string
                  required:
                  - events
                  type: object
                type: array
              policy:
                description: Policy controls retry, history, and suspend behavior.
                properties:
                  failurePolicy:
                    allOf:
                    - enum:
                      - Fail
                      - Ignore
                    - enum:
                      - Fail
                      - Ignore
                    description: |-
                      FailurePolicy controls behavior when the provider returns an error.
                      Fail marks the policy as Failed; Ignore logs the error and continues.
                    type: string
                  historyLimit:
                    description: |-
                      HistoryLimit is the maximum number of RotationRecords to retain per policy.
                      Defaults to 10.
                    format: int32
                    minimum: 1
                    type: integer
                  retryBackoff:
                    description: RetryBackoff is the delay between retry attempts.
                    type: string
                  retryLimit:
                    description: RetryLimit is the maximum number of retry attempts
                      on transient failure.
                    format: int32
                    minimum: 0
                    type: integer
                  suspend:
                    description: Suspend pauses all scheduled rotations without deleting
                      the policy.
                    type: boolean
                type: object
              provider:
                description: Provider configures the secret backend to fetch rotated
                  values from.
                properties:
                  configRef:
                    description: ConfigRef optionally references a ConfigMap or Secret
                      with provider configuration.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  name:
                    description: 'Name is the provider type: mock, static, aws, vault.'
                    enum:
                    - mock
                    - static
                    - aws
                    - vault
                    type: string
                  params:
                    additionalProperties:
                      type: string
                    description: Params contains non-sensitive, provider-specific
                      configuration parameters.
                    type: object
                required:
                - name
                type: object
              restartTargets:
                description: RestartTargets lists workloads to rolling-restart after
                  a successful rotation.
                items:
                  description: RestartTarget identifies a workload to rolling-restart
                    after rotation.
                  properties:
                    kind:
                      description: 'Kind is one of: Deployment, StatefulSet, DaemonSet.'
                      enum:
                      - Deployment
                      - StatefulSet
                      - DaemonSet
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              rotationWindow:
                description: RotationWindow is how early before the scheduled time
                  to trigger rotation.
                type: string
              schedule:
                description: Schedule is a standard cron expression defining when
                  to rotate.
                pattern: ^(@(annually|yearly|monthly|weekly|daily|hourly)|((\*|[0-9,\-\*\/]+)\s){4}(\*|[0-9,\-\*\/]+))$
                type: string
              secretRef:
                description: SecretRef is the Kubernetes secret to create or update
                  with rotated values.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
            required:
            - keys
            - provider
            - schedule
            - secretRef
            type: object
          status:
            description: SecretRotationPolicyStatus defines the observed state of
              SecretRotationPolicy.
            properties:
              conditions:
                description: Conditions represents the latest observations of the
                  policy's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentRetryCount:
                description: CurrentRetryCount is the number of retry attempts for
                  the current rotation.
                format: int32
                type: integer
              lastRotationRecord:
                description: LastRotationRecord references the most recent RotationRecord.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              lastRotationTime:
                description: LastRotationTime is the timestamp of the most recent
                  successful rotation.
                format: date-time
                type: string
              nextRotationTime:
                description: NextRotationTime is the timestamp of the next scheduled
                  rotation.
                format: date-time
                type: string
              phase:
                description: Phase is the current lifecycle phase of this policy.
                enum:
                - Pending
                - Rotating
                - Ready
                - Failed
                - Suspended
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
